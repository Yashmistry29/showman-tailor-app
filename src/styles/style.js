import { StyleSheet } from "react-native";

export const customStyle = StyleSheet.create({
  submitButton: {
    borderWidth: 2,
    borderColor: "#000",
  },
  resetButton: {
    borderWidth: 2,
    borderColor: "#000",
  },
});

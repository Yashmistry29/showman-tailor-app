import { StyleSheet } from "react-native";

export const customStyle = StyleSheet.create({
  submitButton: {
    backgroundColor: "#4c0519",
    borderRadius: 8,
    paddingVertical: 4,
    marginTop: 4,
    borderWidth: 2,
    borderColor: "#000",
  },
  resetButton: {
    borderRadius: 8,
    borderWidth: 2,
    borderColor: "#000",
  },
});

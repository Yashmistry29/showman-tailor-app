import { styled } from 'nativewind';
import { DataTable } from 'react-native-paper';

// Wrap components
const StyledDataTableHeader = styled(DataTable.Header);
const StyledDataTableTitle = styled(DataTable.Title);
const StyledDataTableRow = styled(DataTable.Row);
const StyledDataTableCell = styled(DataTable.Cell);
